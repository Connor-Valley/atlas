atlas.local {
    handle /share* {
        reverse_proxy localhost:3000
    }

    # API routes
    handle /api* {
        reverse_proxy localhost:3000
    }

    # Frontend (default)
    handle {
        reverse_proxy localhost:5173
    }
}
